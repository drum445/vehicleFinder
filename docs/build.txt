Start local MySQL
In "backend/repos/base.go" you may need to change the credentials

Backend:
  $ cd backend
  $ go get
  $ go build
  $ ./backend

Frontend:
  $ cd frontend
  $ npm install
  $ npm run dev
